server:
  port: 8888
spring:
  application:
    name: customer
  datasource:
    password: lopes
    url: jdbc:postgresql://localhost:5432/customerdb
    username: lopes
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
